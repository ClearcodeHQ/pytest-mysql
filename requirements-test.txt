# test runs requirements (versions we'll be testing against) - automatically updated by requires.io
coverage==4.5.4 # pytest-cov
pytest==5.1.0
mirakuru==2.0.1
mysqlclient==1.4.4 # rq.filter: !=1.4.2.post1
port-for==0.4
